name: dora-services
services:
  dora-mariadb:
    image: mariadb:latest
    ports:
      - "3306:3306"
    volumes:
      - mariadb_data:/var/lib/mysql
    env_file:
      - path: "./settings/.mariadb.env"
        required: true
    labels:
      kompose.service.type: nodeport
      kompose.volume.type: hostPath

  dora-chromadb:
    image: chromadb/chroma:latest
    ports:
      - "8001:8000"
    volumes:
      - chroma_data:/data
    environment:
      - IS_PERSISTENT=TRUE
      - ANONYMIZED_TELEMETRY=FALSE
    labels:
      kompose.service.type: nodeport
      kompose.volume.type: hostPath

  dora-backend:
    image: docker.io/library/dora-backend:latest
    build:
      context: .
      dockerfile: Dockerfile
    develop:
      watch:
        - action: rebuild
          path: .
    ports:
      - "8000:8000"
      - "5678:5678"
    restart: "always"
    env_file:
      - path: ".env"
        required: true
      - path: "./settings/.app.env"
        required: true
      - path: "./settings/.retrieval.env"
        required: true
      - path: "./settings/.mariadb.env"
        required: true
      - path: "./settings/.model.env"
        required: true
    environment:
      - CHROMA_SERVER_HOST=${CHROMA_SERVER_HOST:-dora-chromadb}
      - CHROMA_SERVER_PORT=${CHROMA_SERVER_PORT:-8000}
    volumes:
      - logging_file_dir:/app/logs
      - chat_model_folder:/models/chat_models
      - embedding_model_folder:/models/embedding_model
    depends_on:
      - dora-mariadb
      - dora-chromadb
    labels:
      kompose.service.type: nodeport
      kompose.volume.type: hostPath
      kompose.image-pull-policy: "Never"

volumes:
  logging_file_dir:
  mariadb_data:
  chroma_data:
  chat_model_folder:
    driver: local
    driver_opts:
     type: none
     o: bind
     device: ${CHAT_MODEL_FOLDER_PATH:?error}
  embedding_model_folder:
    driver: local
    driver_opts:
     type: none
     o: bind
     device: ${EMBEDDING_MODEL_FOLDER_PATH:?error}
